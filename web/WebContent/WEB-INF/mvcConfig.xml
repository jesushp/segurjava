<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd">
	
	<context:component-scan base-package="controller"/>
	<mvc:annotation-driven>
	    <mvc:async-support default-timeout="30000" task-executor="taskExecutor"/>
	</mvc:annotation-driven>  
<!-- modify the parameters of thread pool -->
  <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
    <property name="corePoolSize" value="5"/>
    <property name="maxPoolSize" value="50"/>
    <property name="queueCapacity" value="10"/>
    <property name="keepAliveSeconds" value="120"/>
  </bean>
	<!-- objeto encargado de resolver la navegación -->
	<bean id="resolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- The JSP based view files are located under the /WEB-INF folder of the project, WEB-INF/view/ 
		so they're only accessible to the Spring infrastructure and not by direct URL access.
		    <mvc:view-controller path="/" view-name="index" />
		-->
		<property name="prefix" value="/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory
	<mvc:resources mapping="/resources/**" location="/resources/" /> -->
	<mvc:resources mapping="/css/**" location="/css/"/>
	<mvc:resources mapping="/img/**" location="/img/"/>
	<mvc:resources mapping="/js/**" location="/js/"/>
	
	<bean id="fechaValidator" class="controller.validator.DateFormValidator" />
	<bean id="messageSource"
		class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="basename" value="classpath:resources/message" />
		<property name="defaultEncoding" value="UTF-8" />
	</bean>
	<bean id="template"
		class="org.springframework.web.client.RestTemplate">
	</bean>
	<mvc:view-controller path="toLogin" view-name="login"/>
	<mvc:view-controller path="toMenu" view-name="cliente-menu"/>
	<mvc:view-controller path="toAdminMenu" view-name="admin-menu"/>
	<mvc:view-controller path="toAdminInformeTemporal" view-name="admin-informeTemporal"/>
	<mvc:view-controller path="toAdminInformeCliente" view-name="admin-informeCliente"/>
</beans>
